<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.ToDoWidget" parent="@android:style/Theme.Material.Light.NoActionBar" />

    <!--
      The add sheet floats over the launcher instead of looking like a separate app screen.
      It deliberately does NOT inherit Theme.Translucent: that sets windowTranslucentStatus and
      windowTranslucentNavigation, which make the platform ignore adjustResize and stop dispatching
      the insets the sheet needs to sit above the navigation bar and the keyboard.
    -->
    <style name="Theme.ToDoWidget.Sheet" parent="@android:style/Theme.Material.NoActionBar">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowIsFloating">false</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowTranslucentStatus">false</item>
        <item name="android:windowTranslucentNavigation">false</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:windowAnimationStyle">@android:style/Animation.InputMethod</item>
    </style>
</resources>
